[workspace]
members = ["internal", "macro"]

[package]
name = "ct-regex"
version = "0.1.0"
edition = "2024"

[dependencies]
ct-regex-internal = { path = "internal" }
ct-regex-macro = { path = "macro"}
regex-automata = "0.4.14"
